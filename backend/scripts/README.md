# Scripts

- `python -m scripts.seed`: nạp JSON/Markdown/TXT trong `data/seed` vào ChromaDB.
- `python -m scripts.reset_onboarding --author-id ID --channel-id ID`: reset
  Story 4 cho đúng một học viên/channel trước khi demo lại.
- `python -m src.bootstrap.main`: seed (nếu bật) rồi khởi chạy Discord bot.
